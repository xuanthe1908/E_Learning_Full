// Mock Mode Configuration
// Set to true to use mock data instead of API calls (for reporting/demo)
export const USE_MOCK_DATA = false; // ✅ Tắt mock mode, sử dụng data từ database

// Delay simulation (ms) - để giả lập network delay
export const MOCK_DELAY = 500;























